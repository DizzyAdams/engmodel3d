import type { BriefSubmission } from "../server/briefing.js";
import type { CatalogListing } from "../server/model-catalog.js";
import type { ProjectRecord } from "../server/mock-data.js";

const h = (value: unknown) => {
  const text = String(value ?? "");
  return text.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#39;");
};
const j = (value: unknown) => JSON.stringify(value).replace(/</g, "\\u003c");
const nav = `<nav class="nav" aria-label="Workspace navigation"><a href="/">Home</a><a href="/projects/casa-contemporanea">3D workbench</a><a href="/mission-control">Run mission</a><a href="/solutions">Solutions</a><a href="/workflow">Workflow</a><a href="/packages">Packages</a><a href="/roadmap">Roadmap</a><a href="/briefs">Saved briefs</a></nav>`;

const css = `<style>
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Space+Grotesk:wght@400;500;600;700&display=swap');
:root{color-scheme:dark;--bg:#04060b;--panel:rgba(14,20,36,.72);--panel-2:#0b1222;--border:rgba(159,182,255,.14);--border-strong:rgba(124,243,255,.22);--text:#f5f8ff;--muted:#8fa3bf;--accent:#7cf3ff;--accent-2:#7c9bff;--green:#78f4b9;--warning:#ffd36d;--danger:#ff8d9b;--radius:26px;--radius-sm:18px;--shadow:0 34px 110px #0000005c;--glow:0 0 90px #7cf3ff1a;--font:"Space Grotesk","Inter",Segoe UI,Arial,sans-serif}
*{box-sizing:border-box}
::selection{background:#7cf3ff2a;color:#fff}
html,body{min-height:100%}
body{margin:0;font-family:var(--font);color:var(--text);background:radial-gradient(circle at 12% 10%,#12394e 0,transparent 26%),radial-gradient(circle at 86% 14%,#241d58 0,transparent 30%),radial-gradient(circle at 50% 110%,#0c2a26 0,transparent 34%),linear-gradient(180deg,#05090f,#020408);background-attachment:fixed}
body::before,body::after{content:"";position:fixed;inset:0;pointer-events:none;z-index:-2}
body::before{background:radial-gradient(circle at 16% 12%,#7cf3ff14,transparent 22%),radial-gradient(circle at 84% 10%,#7c9bff14,transparent 24%);filter:blur(14px)}
body::after{z-index:-1;opacity:.18;background-image:linear-gradient(#ffffff08 1px,transparent 1px),linear-gradient(90deg,#ffffff07 1px,transparent 1px);background-size:110px 110px;mask-image:radial-gradient(circle at center,#000 28%,transparent 86%)}
main{max-width:1420px;margin:0 auto;padding:28px 22px 80px}
a{color:var(--accent);text-decoration:none}
.skip-link{position:fixed;top:14px;left:14px;z-index:20;transform:translateY(-180%);padding:10px 14px;border-radius:14px;background:var(--accent);color:#041018;font-weight:800;transition:transform 180ms cubic-bezier(.32,.72,0,1)}
.skip-link:focus{transform:translateY(0)}
.panel{position:relative;background:linear-gradient(180deg,var(--panel),rgba(12,18,28,.72));border:1px solid var(--border);border-radius:var(--radius);box-shadow:var(--shadow),inset 0 1px 0 #ffffff0a;backdrop-filter:blur(18px);padding:22px;margin:18px 0;overflow:hidden}
.panel::after{content:"";position:absolute;inset:0;pointer-events:none;border-radius:inherit;background:linear-gradient(180deg,#ffffff0f,transparent 22%);opacity:.55}
.grid{display:grid;gap:14px}
.c2{grid-template-columns:repeat(2,minmax(0,1fr))}.c3{grid-template-columns:repeat(3,minmax(0,1fr))}.c4{grid-template-columns:repeat(4,minmax(0,1fr))}
.card{padding:16px;border:1px solid rgba(255,255,255,.08);border-radius:var(--radius-sm);background:rgba(255,255,255,.04);backdrop-filter:blur(14px);box-shadow:0 18px 40px #0000002b;transition:transform 200ms cubic-bezier(.32,.72,0,1),border-color 200ms ease,box-shadow 200ms ease}
.card:hover{transform:translateY(-3px);border-color:var(--border-strong);box-shadow:0 24px 60px #00000038,0 0 0 1px #7cf3ff14}
.card__title{font-size:18px;line-height:1.15;letter-spacing:-.02em;margin:8px 0}
.muted,.tiny{color:var(--muted)}
.tiny{font-size:12px;letter-spacing:.08em;text-transform:uppercase}
.row{display:flex;justify-content:space-between;gap:18px;align-items:flex-start}
.nav{display:flex;gap:8px;flex-wrap:wrap;margin-top:18px}
.nav a,.pill,.btn{padding:9px 13px;border:1px solid rgba(255,255,255,.1);border-radius:999px;background:#ffffff08;backdrop-filter:blur(12px)}
.pill{display:inline-block;color:var(--accent);font-size:12px;letter-spacing:.06em;text-transform:uppercase;font-weight:600}
.btn{display:inline-flex;align-items:center;justify-content:center;gap:8px;color:var(--text);cursor:pointer;font-weight:600;transition:transform 180ms cubic-bezier(.32,.72,0,1),border-color 180ms ease,background 180ms ease,box-shadow 180ms ease}
.btn:hover{transform:translateY(-2px);border-color:var(--border-strong);background:#ffffff0f;box-shadow:0 14px 30px #0000002e}
.btn.primary{background:linear-gradient(135deg,#7cf3ff1c,#7cf3ff0f);border-color:#7cf3ff55;box-shadow:0 0 40px #7cf3ff0d}
.btn.primary::before{content:"";position:absolute;inset:0;border-radius:inherit;background:linear-gradient(135deg,#7cf3ff26,transparent);opacity:0;transition:opacity 200ms ease}
.btn.primary:hover::before{opacity:1}
.section{margin-top:28px}
.section h2,.section h3{margin:0 0 14px;letter-spacing:-.02em}
.tag{display:inline-flex;align-items:center;padding:6px 10px;border-radius:999px;background:#7c9bff14;color:#b9c7ff;font-size:12px;backdrop-filter:blur(8px);border:1px solid #7c9bff14}
.status{display:inline-flex;padding:8px 12px;border-radius:999px;background:#78f4b91a;color:#78f4b9;font-size:12px;backdrop-filter:blur(8px);border:1px solid #78f4b91a}
.warn{color:#ffd36d;background:#ffd36d14;border-color:#ffd36d14}
input,select,textarea{border-color:var(--border);border-radius:10px;background:#080c16;color:var(--text);padding:10px 12px}
.hero{display:grid;grid-template-columns:minmax(0,1.05fr) minmax(0,.95fr);gap:18px;align-items:stretch;padding:26px;border-radius:34px;background:linear-gradient(135deg,#0f1c33,#0a1224 55%,#070e1a);border:1px solid #ffffff0f;box-shadow:inset 0 1px 0 #ffffff0d,0 40px 100px #00000047}
.hero__eyebrow{display:inline-flex;align-items:center;gap:10px;padding:8px 12px;border-radius:999px;background:#7cf3ff12;border:1px solid #7cf3ff26;color:var(--accent);font-size:12px;letter-spacing:.12em;text-transform:uppercase}
.hero h1{font-size:clamp(36px,6.2vw,78px);line-height:.94;margin:14px 0;letter-spacing:-.045em;background:linear-gradient(180deg,#ffffff,#90a7ff8c);-webkit-background-clip:text;background-clip:text;-webkit-text-fill-color:transparent}
.hero h2{font-size:clamp(22px,3.6vw,42px);margin:10px 0;letter-spacing:-.03em;line-height:1.1}
.hero p,.card p{line-height:1.6}
.hero__actions{display:flex;flex-wrap:wrap;gap:10px;margin-top:16px}
.metrics{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:12px}
.metric{padding:18px;border-radius:var(--radius-sm);background:linear-gradient(180deg,rgba(255,255,255,.04),rgba(255,255,255,.02));border:1px solid rgba(255,255,255,.08);backdrop-filter:blur(14px);position:relative;overflow:hidden}
.metric::before{content:"";position:absolute;inset:0;background:radial-gradient(circle at top left,#7cf3ff14,transparent 55%);opacity:0;transition:opacity 200ms ease}
.metric:hover::before{opacity:1}
.metric strong{display:block;margin-top:10px;font-size:26px;letter-spacing:-.02em;font-variant-numeric:tabular-nums}
.stage{position:relative;min-height:460px;overflow:hidden;border-radius:24px;border:1px solid #7cf3ff24;background:radial-gradient(circle at 50% 38%,#7cf3ff12,transparent 24%),radial-gradient(circle at 35% 30%,#7c9bff12,transparent 36%),linear-gradient(180deg,#0e1732,#050a14);box-shadow:inset 0 0 0 1px #ffffff08,0 30px 80px #00000042}
.stage canvas{width:100%;height:100%;display:block}
.stage::after{content:"";position:absolute;inset:0;pointer-events:none;background:linear-gradient(#ffffff0d 1px,transparent 1px),linear-gradient(90deg,#ffffff0a 1px,transparent 1px);background-size:54px 54px;mask-image:linear-gradient(180deg,#000 20%,transparent)}
.controls{display:grid;gap:14px}
.control{display:grid;gap:8px}
.control span{color:var(--muted);font-size:13px}
.control strong{color:var(--text);font-variant-numeric:tabular-nums}
.control input,.control select{width:100%;accent-color:var(--accent)}
.actions{display:flex;flex-wrap:wrap;gap:10px;margin-top:16px}
.status{display:inline-flex;padding:8px 12px;border-radius:999px;background:#78f4b91a;color:#78f4b9;font-size:12px;backdrop-filter:blur(8px);border:1px solid #78f4b91a}
.viewer-grid{display:grid;grid-template-columns:minmax(0,1.25fr) minmax(0,.75fr);gap:16px}
.table{display:grid;gap:10px}
.table .card{display:flex;justify-content:space-between;gap:14px;align-items:center}
.bento{display:grid;gap:14px;grid-template-columns:repeat(3,minmax(0,1fr))}
.bento__lead{grid-column:span 2;background:linear-gradient(135deg,#7cf3ff0f,#7c9bff0f);border:1px solid #7cf3ff14;box-shadow:none}
.bento__card{padding:18px;border-radius:var(--radius-sm);background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.08);backdrop-filter:blur(14px);transition:transform 200ms ease,border-color 200ms ease,box-shadow 200ms ease}
.bento__card:hover{transform:translateY(-2px);border-color:#7cf3ff2a;box-shadow:0 22px 50px #00000030}
.workflow-rail{display:grid;gap:18px;background:#0a1222;border-radius:var(--radius);border:1px solid #ffffff08;padding:18px;margin-top:18px}
.workflow-rail .grid{gap:10px}.workflow-rail .card{padding:14px}.workflow-rail h2{margin:6px 0}.workflow-rail .actions{margin-top:0}@media(max-width:900px){.hero,.viewer-grid,.bento,.c2,.c3,.c4{grid-template-columns:1fr}.metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.row{flex-direction:column}}@media(max-width:640px){main{padding:16px 14px 48px}.nav{overflow-x:auto;flex-wrap:nowrap}.nav a{white-space:nowrap}.hero h1{font-size:48px}.metrics{grid-template-columns:repeat(2,minmax(0,1fr))}.bento{grid-template-columns:1fr}.bento__lead{grid-column:span 1}}
@keyframes fadeUp{0%{opacity:0;transform:translateY(18px)}100%{opacity:1;transform:translateY(0)}}
@keyframes heroPulse{0%,100%{filter:drop-shadow(0 0 28px #7cf3ff18)}50%{filter:drop-shadow(0 0 52px #7c9bff26)}}
@keyframes borderShimmer{0%{background-position:0% 50%}50%{background-position:100% 50%}100%{background-position:0% 50%}}
.hero{animation:heroPulse 10s ease-in-out infinite}
.card,.bento__card{animation:fadeUp 420ms cubic-bezier(.32,.72,0,1) both}
.metrics .card,.bento .bento__card{animation-delay:60ms}
.stage{border:1px solid transparent;background-image:linear-gradient(#0e1732,#0e1732),linear-gradient(135deg,#7cf3ff26,#7c9bff14);background-origin:border-box;background-clip:padding-box,border-box}
.btn.primary{position:relative;overflow:hidden;isolation:isolate}
</style>`;

export function page(title: string, body: string, scripts = "") { return `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${h(title)} · Model3DEng</title>${css}</head><body><a class="skip-link" href="#main-content">Skip to main content</a><main id="main-content">${body}</main>${scripts}</body></html>`; }
const cards = (items: Array<{label?: string; title?: string; value?: string; detail?: string}>) => `<div class="grid c3">${items.map((x) => `<article class="card"><div class="tiny">${h(x.label ?? "")}</div><h3>${h(x.title ?? x.value ?? "")}</h3><p class="muted">${h(x.detail ?? "")}</p></article>`).join("")}</div>`;

export function dashboard(data: ReturnType<typeof import("../server/mock-data.js").getDashboardData>) { return page("Dashboard", `<section class="panel hero"><div><span class="pill">Construction 3D generation</span><h1>Model3DEng</h1><p class="muted">Generate houses, towers, commercial spaces and interiors from prompt, PNG/JPEG, video, drone footage or listing media, with BIM/IFC, approval and sales-ready delivery.</p><div class="actions"><a class="btn primary" href="/projects/casa-contemporanea">Open 3D workbench</a><a class="btn" href="/#multimodal">Multimodal inputs</a><a class="btn" href="/mission-control">Run agents</a></div></div><div class="stage"><div style="padding:22px"><span class="status">26-agent construction swarm</span><h2>From raw media to buildable, sellable model.</h2><p class="muted">Vision, reconstruction, BIM, interiors, rendering and commercial packaging in one governed queue.</p></div></div></section><section class="metrics">${data.stats.map((s) => `<article class="panel metric"><span class="tiny">${h(s.label)}</span><strong>${h(s.value)}</strong></article>`).join("")}</section><section class="panel section"><h2>Ready construction demos</h2><div class="grid c4">${data.marketplaceListings.map((x) => `<article class="card"><div class="tiny">${h(x.category)} · ${h(x.license)}</div><h3>${h(x.name)}</h3><p class="muted">${h(x.signal)}</p><span class="tag">${h(x.price)}</span><span class="tag">${h(x.formats)}</span><span class="status">${h(x.qualityScore)} quality</span></article>`).join("")}</div></section><section class="panel section" id="multimodal"><h2>Prompt, images, video, drone and existing listings</h2><div class="grid c3">${data.multimodalInputModes.map((x) => `<article class="card"><div class="tiny">${h(x.input)} · ${h(x.agent)}</div><h3>${h(x.name)}</h3><p class="muted">${h(x.detail)}</p><span class="tag">${h(x.output)}</span><span class="status">${h(x.confidence)}</span></article>`).join("")}</div></section><section class="panel section"><h2>Generation workflow</h2><div class="grid c4">${data.generationWorkflow.map((x) => `<article class="card"><div class="tiny">${h(x.stage)} · ${h(x.gate)}</div><h3>${h(x.title)}</h3><p class="muted">${h(x.input)}</p><span class="tag">${h(x.output)}</span></article>`).join("")}</div></section><section class="panel section"><h2>Construction operating lanes</h2><div class="grid c4">${data.marketplaceLanes.map((x) => `<article class="card"><div class="tiny">${h(x.status)} · ${h(x.owner)}</div><h3>${h(x.name)}</h3><p class="muted">${h(x.detail)}</p><span class="status">${h(x.count)}</span></article>`).join("")}</div></section><section class="panel section"><h2>Open source leverage</h2><div class="grid c3">${data.openSourceReferences.map((x) => `<article class="card"><div class="tiny">${h(x.domain)}</div><h3>${h(x.name)}</h3><p class="muted">${h(x.adoption)}</p><span class="tag">${h(x.feature)}</span></article>`).join("")}</div></section><section class="panel section"><h2>Active projects</h2><div class="grid c2">${data.projects.map((p) => `<a class="card" href="/projects/${h(p.id)}"><div class="tiny">${h(p.category)}</div><h3>${h(p.name)}</h3><p class="muted">${h(p.summary)}</p><span class="status">${h(p.status)}</span></a>`).join("")}</div></section>${nav}`); }

function projectViewer(project: ProjectRecord) {
  return `<section class="panel" id="project-preview"><div class="row"><div><span class="pill">Live WebGL / procedural solid</span><h2>Surreal 3D workbench</h2><p class="muted">Orbit, tune dimensions, inspect metrics and download real STL/GLB/JSON artifacts.</p></div><span id="viewer-status" class="status">Starting viewer…</span></div><div class="viewer-grid"><div id="viewer-stage" class="stage" aria-label="${h(project.name)} interactive 3D model"></div><div class="card"><div class="controls"><label class="control"><span>Base length <strong id="v-width">220</strong> mm</span><input id="width" type="range" min="120" max="360" step="5" value="220"></label><label class="control"><span>Leg height <strong id="v-height">80</strong> mm</span><input id="height" type="range" min="40" max="180" step="2" value="80"></label><label class="control"><span>Depth <strong id="v-depth">32</strong> mm</span><input id="depth" type="range" min="12" max="60" step="1" value="32"></label><label class="control"><span>Material</span><select id="material"><option value="aluminum">Aluminum</option><option value="steel">Steel</option><option value="nylon">Nylon</option></select></label></div><div class="grid c2 section"><div class="card"><div class="tiny">Volume</div><strong id="volume">—</strong></div><div class="card"><div class="tiny">Mass</div><strong id="mass">—</strong></div><div class="card"><div class="tiny">Aspect</div><strong id="aspect">—</strong></div><div class="card"><div class="tiny">Mesh</div><strong id="mesh">ready</strong></div></div><div class="actions"><button class="btn primary" id="download-stl">Download STL</button><button class="btn" id="download-glb">Download GLB</button><button class="btn" id="download-json">Download JSON</button></div></div></div></section><section class="panel section" id="export-readiness"><h2>Release posture</h2><div class="grid c3">${project.exportReadiness.map((x) => `<article class="card"><div class="tiny">${h(x.format)} · ${h(x.priority)}</div><h3>${h(x.status)}</h3><p class="muted">${h(x.note)}</p></article>`).join("")}</div></section>`;
}

function viewerScript(project: ProjectRecord) { return `<script type="module">import * as THREE from 'https://unpkg.com/three@0.185.1/build/three.module.js';import {OrbitControls} from 'https://unpkg.com/three@0.185.1/examples/jsm/controls/OrbitControls.js';import {STLExporter} from 'https://unpkg.com/three@0.185.1/examples/jsm/exporters/STLExporter.js';import {GLTFExporter} from 'https://unpkg.com/three@0.185.1/examples/jsm/exporters/GLTFExporter.js';const P=${j({id:project.id,name:project.name})},$=id=>document.getElementById(id),stage=$('viewer-stage');let W=220,H=80,D=32,material='aluminum',root,mesh,renderer,scene,camera,controls;const color={aluminum:0x7cf3ff,steel:0x7c9bff,nylon:0x78f4b9};function geom(){const s=new THREE.Shape();s.moveTo(0,0);s.lineTo(W/100,0);s.lineTo(W/100,D/1000);s.lineTo(18/1000,D/1000);s.lineTo(18/1000,H/100);s.lineTo(0,H/100);s.closePath();const g=new THREE.ExtrudeGeometry(s,{depth:D/1000,bevelEnabled:true,bevelSegments:3,bevelSize:.012,bevelThickness:.012,curveSegments:16});g.center();return g}function rebuild(){if(mesh){mesh.geometry.dispose();root.remove(mesh)}mesh=new THREE.Mesh(geom(),new THREE.MeshStandardMaterial({color:color[material],metalness:.55,roughness:.24,emissive:color[material],emissiveIntensity:.05}));root.add(mesh);$('v-width').textContent=W;$('v-height').textContent=H;$('v-depth').textContent=D;$('volume').textContent=((W*H*D*.62)/1000).toFixed(1)+' cm³';$('mass').textContent=(((W*H*D*.62)/1000*(material==='steel'?7.85:material==='nylon'?1.15:2.7))/1000).toFixed(2)+' kg';$('aspect').textContent=(W/Math.max(H,1)).toFixed(2)+':1'}function download(name,blob){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}function init(){if(!stage||!THREE.WebGLRenderer){return}scene=new THREE.Scene();camera=new THREE.PerspectiveCamera(38,1,.1,100);camera.position.set(3.8,2.8,5.2);renderer=new THREE.WebGLRenderer({antialias:true,alpha:true});renderer.setPixelRatio(Math.min(devicePixelRatio||1,1.5));stage.appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0xd9f0ff,0x172038,1.8));const key=new THREE.DirectionalLight(0xffffff,2.8);key.position.set(4,7,5);scene.add(key);const accent=new THREE.PointLight(0x7cf3ff,8,10);accent.position.set(-3,2,-2);scene.add(accent);root=new THREE.Group();scene.add(root);const ring=new THREE.Mesh(new THREE.TorusGeometry(1.8,.012,8,96),new THREE.MeshBasicMaterial({color:0x7cf3ff,transparent:true,opacity:.5}));ring.rotation.x=Math.PI/2;root.add(ring);rebuild();controls=new OrbitControls(camera,renderer.domElement);controls.enableDamping=true;controls.target.set(0,.1,0);const resize=()=>{const w=stage.clientWidth,h=Math.max(stage.clientHeight,1);camera.aspect=w/h;camera.updateProjectionMatrix();renderer.setSize(w,h,false)};new ResizeObserver(resize).observe(stage);resize();$('viewer-status').textContent='Live model active';function tick(){root.rotation.y+=.003;ring.rotation.z+=.006;controls.update();renderer.render(scene,camera);requestAnimationFrame(tick)}tick()}$('width').oninput=e=>{W=+e.target.value;rebuild()};$('height').oninput=e=>{H=+e.target.value;rebuild()};$('depth').oninput=e=>{D=+e.target.value;rebuild()};$('material').onchange=e=>{material=e.target.value;rebuild()};$('download-json').onclick=()=>download(P.id+'.engineering-state.json',new Blob([JSON.stringify({project:P,width:W,height:H,depth:D,material,generatedAt:new Date().toISOString()},null,2)],{type:'application/json'}));$('download-stl').onclick=()=>download(P.id+'.stl',new Blob([new STLExporter().parse(mesh)],{type:'model/stl'}));$('download-glb').onclick=()=>new GLTFExporter().parse(root,r=>download(P.id+'.glb',new Blob([r],{type:'model/gltf-binary'})),e=>alert('GLB export failed'),{binary:true});try{init()}catch(e){$('viewer-status').textContent='Fallback mode';$('viewer-status').classList.add('warn');stage.innerHTML='<div style="padding:24px;color:#9eafce">WebGL indisponível neste dispositivo. Os dados e o fluxo continuam acessíveis.</div>'}</script>`; }

export function project(project: ProjectRecord) { return page(project.name, `<section class="panel hero"><div><span class="pill">${h(project.category)}</span><h1>${h(project.name)}</h1><p class="muted">${h(project.summary)}</p><div class="actions"><span class="status">${h(project.status)}</span><span class="pill">${h(project.validationState)}</span><span class="pill">${h(project.exportTarget)}</span></div></div><div class="card"><div class="tiny">Next action</div><h2>${h(project.nextAction)}</h2><p class="muted">Owner: ${h(project.owner)} · Revision: ${h(project.lastRevision)}</p></div></section>${projectViewer(project)}<section class="panel section" id="project-analysis"><h2>Engineering signals</h2>${cards(Object.entries(project.engineering).map(([label,value])=>({label,title:value})))}</section><section class="panel section" id="validation"><h2>Validation and activity</h2><div class="grid c2">${project.validationChecks.map(x=>`<article class="card"><strong>${h(x.name)}</strong><p class="muted">${h(x.result)}</p><span class="status">${h(x.status)}</span></article>`).join("")}${project.activity.map(x=>`<article class="card"><strong>${h(x.title)}</strong><p class="muted">${h(x.detail)}</p><div class="tiny">${h(x.timestamp)}</div></article>`).join("")}</div></section><div id="project-versions"></div>${nav}`, viewerScript(project)); }

export function sprints(data: ReturnType<typeof import("../server/mock-data.js").getDashboardData>, sprintItems: Array<{index:number;owner:string;title:string;goal:string}>) { return page("Sprints", `<section class="panel hero"><div><span class="pill">Execution map</span><h1>10 sprints</h1><p class="muted">Platform hardening, 3D viewer, governance, export and deployment readiness.</p></div></section><div class="grid c2">${sprintItems.map(x=>`<article class="panel"><div class="tiny">Sprint ${h(x.index)} · ${h(x.owner)}</div><h2>${h(x.title)}</h2><p class="muted">${h(x.goal)}</p></article>`).join("")}</div>${nav}`); }
function missionScript() { return `<script>
const $=id=>document.getElementById(id), state={queued:0,running:0,done:0,failed:0,events:[]};
function render(){ for(const key of ['queued','running','done','failed']) $(key+'-count').textContent=state[key]; $('event-feed').innerHTML=state.events.slice(-30).reverse().map(e=>'<article class="card"><div class="tiny">'+new Date(e.emittedAt).toLocaleTimeString()+' · '+e.type+'</div><strong>'+((e.title||e.role||'System event').replace(/[&<>]/g,''))+'</strong><p class="muted">'+((e.output||e.error||e.projectId||'heartbeat').replace(/[&<>]/g,''))+'</p></article>').join(''); }
function push(e){ if(e.type==='task.queued'||e.type==='task.running'||e.type==='task.done'||e.type==='task.failed'){state[e.type.split('.')[1]]++; if(e.type==='task.running')state.queued=Math.max(0,state.queued-1); if(e.type==='task.done'||e.type==='task.failed')state.running=Math.max(0,state.running-1); } state.events.push(e); render(); }
const stream=new EventSource('/api/agents/events'); stream.onopen=()=>{$('stream-status').textContent='LIVE · SSE connected';$('stream-status').className='status live'}; stream.onmessage=e=>{try{push(JSON.parse(e.data))}catch{}}; stream.onerror=()=>{$('stream-status').textContent='RECONNECTING · SSE';$('stream-status').className='status'};
$('start-run').onclick=async()=>{ $('start-run').disabled=true; const r=await fetch('/api/agents/run',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({project:{name:'Mission Control Run',kind:'mechanical',goal:'Generate and validate a production-ready component',tags:['live']},brief:'Build a governed engineering concept with measurable validation and export readiness.',constraints:{width:220,height:80,thickness:32}})}); $('run-result').textContent=r.ok?'14-agent run queued':'Run rejected: '+await r.text(); $('start-run').disabled=false; };
$('claim-task').onclick=async()=>{const r=await fetch('/api/agents/tasks/claim',{method:'POST'});$('run-result').textContent=r.status===200?'Next task claimed and marked running.':'No queued task available.'};
render();
</script>`; }
export function mission(data: ReturnType<typeof import("../server/mock-data.js").getDashboardData>) { return page("Mission Control", `<section class="panel hero"><div><span class="pill">Realtime orchestration</span><h1>Mission control</h1><p class="muted">${h(data.swarmMetrics.map(x=>x.value+' '+x.label).join(' · '))}</p><div class="actions"><button id="start-run" class="btn primary">Start 14-agent run</button><button id="claim-task" class="btn">Claim next task</button></div><p id="run-result" class="muted"></p></div><div class="card"><h2>14 lanes alive</h2><p class="muted">Geometry, QA, benchmark, cost, compliance and delivery in one room.</p><span id="stream-status" class="status">CONNECTING · SSE</span></div></section><section class="panel"><h2>Live task state</h2><div class="metrics"><article class="card"><div class="tiny">Queued</div><strong id="queued-count">0</strong></article><article class="card"><div class="tiny">Running</div><strong id="running-count">0</strong></article><article class="card"><div class="tiny">Done</div><strong id="done-count">0</strong></article><article class="card"><div class="tiny">Failed</div><strong id="failed-count">0</strong></article></div></section><section class="panel"><div class="row"><h2>Agent swarm</h2><span class="tiny">Event stream · last 30</span></div><div id="event-feed" class="grid c3"><p class="muted">Waiting for the first event…</p></div></section>${nav}`, missionScript()); }
export function solutions(data: ReturnType<typeof import("../server/mock-data.js").getDashboardData>) { return page("Solutions", `<section class="panel hero"><div><span class="pill">Buyer-facing engineering offers</span><h1>Solutions</h1><p class="muted">One governed workflow for mechanical, BIM, MEP, civil and industrial teams.</p></div></section><section class="panel"><h2>Target sectors</h2><div class="grid c3">${data.sectors.map(x=>`<article class="card"><div class="tiny">${h(x.buyer)}</div><h3>${h(x.name)}</h3><p class="muted">${h(x.pain)}</p><p>${h(x.win)}</p><span class="tag">${h(x.formats)}</span></article>`).join("")}</div></section>${nav}`); }
export function workflow(data: ReturnType<typeof import("../server/mock-data.js").getDashboardData>) { return page("Workflow", `<section class="panel hero"><div><span class="pill">Brief to release</span><h1>Workflow</h1><p class="muted">Every stage has an owner, signal and release consequence.</p></div></section><div class="grid c2">${data.workflow.map(x=>`<article class="panel"><div class="tiny">${h(x.stage)} · ${h(x.owner)}</div><h2>${h(x.title)}</h2><p class="muted">${h(x.outcome)}</p><span class="status">${h(x.signal)}</span></article>`).join("")}</div>${nav}`); }
export function packages(data: ReturnType<typeof import("../server/mock-data.js").getDashboardData>) { return page("Packages", `<section class="panel hero"><div><span class="pill">Commercial entry points</span><h1>Packages</h1><p class="muted">Clear scope, deliverables and outcome for the first paid engagement.</p></div></section><div class="grid c3">${data.packages.map(x=>`<article class="panel"><div class="tiny">${h(x.timeline)}</div><h2>${h(x.name)}</h2><h3>${h(x.price)}</h3><p class="muted">${h(x.fit)}</p><div>${x.deliverables.map(y=>`<span class="tag">${h(y)}</span>`).join("")}</div><p>${h(x.outcome)}</p></article>`).join("")}</div>${nav}`); }
export function roadmap(data: ReturnType<typeof import("../server/mock-data.js").getDashboardData>) { return page("Roadmap", `<section class="panel hero"><div><span class="pill">MVP expansion</span><h1>Roadmap</h1><p class="muted">From surreal cockpit to production engineering operating system.</p></div></section><div class="grid c3">${data.roadmap.map(x=>`<article class="panel"><div class="tiny">${h(x.phase)}</div><h2>${h(x.title)}</h2><p class="muted">${h(x.detail)}</p></article>`).join("")}</div>${nav}`); }
export function briefs(briefsData: BriefSubmission[]) { return page("Briefs", `<section class="panel hero"><div><span class="pill">Local persisted intake</span><h1>Saved briefs</h1><p class="muted">Briefs are escaped before rendering and remain available for the next governed run.</p></div></section>${briefsData.length?`<div class="grid c2">${briefsData.map(b=>`<article class="panel"><div class="tiny">${h(b.id)} · ${h(b.createdAt)}</div><h2>${h(b.form.projectName)}</h2><p class="muted">${h(b.analysis.recommendedSector)} · ${h(b.analysis.recommendedPackage)}</p><div class="card"><strong>Next step</strong><p class="muted">${h(b.analysis.nextStep)}</p></div><div class="card"><strong>Surreal direction</strong><p class="muted">${h(b.analysis.surrealDirection)}</p></div></article>`).join("")}</div>`:'<section class="panel"><h2>No saved briefs yet</h2></section>'}${nav}`); }
export function notFound(path: string) { return page("Not found", `<section class="panel"><h1>Not found</h1><p class="muted">${h(path)}</p>${nav}</section>`); }
const list = (items: string[]) => items.map((item) => `<li>${h(item)}</li>`).join("");

export function catalogFromListings(listings: CatalogListing[]) {
  const grid = listings.map((item) => `<article class="card"><div class="row"><div><div class="tiny">${h(item.category)} · ${h(item.license)}</div><h3>${h(item.name)}</h3></div><span class="status">${h(item.qualityScore)}/100 QA</span></div><p class="muted">${h(item.signal)}</p><div class="section"><span class="tag">${h(item.price)}</span><span class="tag">${h(item.formats)}</span><span class="tag">${h(item.lod)}</span></div><div class="card" style="margin-top:12px"><div class="tiny">Buyer</div><strong>${h(item.buyer)}</strong><p class="muted">${h(item.useCase)}</p></div><div class="actions"><a class="btn primary" href="/catalog/${h(item.slug)}">Open validated package</a><a class="btn" href="/briefs">Start governed brief</a></div></article>`).join("");
  return page("3D Model Catalog", `<section class="panel hero"><div><span class="pill">Enterprise AEC model marketplace</span><h1>Validated 3D catalog</h1><p class="muted">Construction, BIM, commercial-shell, interiors and hospitality packages validated for buyer, format, LOD, units, tolerance, delivery assets and compliance posture.</p><div class="actions"><a class="btn primary" href="/briefs">Start governed brief</a><a class="btn" href="/mission-control">Run specialist agents</a></div></div><div class="card"><div class="tiny">Release gate</div><h2>${h(listings.length)} / ${h(listings.length)} packages mapped</h2><p class="muted">Every listing now carries enterprise buyer, use-case, LOD, tolerance, source input, QA and delivery metadata.</p></div></section><section class="panel section"><div class="row"><div><h2>Construction-ready model packs</h2><p class="muted">Each card opens a detail page with validation matrix and delivery contract.</p></div><span class="status">Slug routes verified</span></div><div class="grid c3">${grid}</div></section>${nav}`);
}

export function catalog(data: ReturnType<typeof import("../server/mock-data.js").getDashboardData>) {
  const listings = data.marketplaceListings ?? [];
  const grid = listings.map((item) => `<article class="card"><div class="tiny">${h(item.category)} · ${h(item.license)}</div><h3>${h(item.name)}</h3><p class="muted">${h(item.signal)}</p><span class="tag">${h(item.price)}</span><span class="tag">${h(item.formats)}</span><span class="status">${h(item.qualityScore)} quality</span></article>`).join("");
  return page("3D Model Catalog", `<section class="panel hero"><div><span class="pill">Ready-to-buy models</span><h1>3D Model catalog</h1><p class="muted">Buyer-ready residential, commercial, interior and BIM packages with validated geometry and PBR-ready exports.</p></div></section><section class="panel section"><div class="grid c3">${grid}</div></section>${nav}`);
}

export function catalogDetail(listing: CatalogListing) {
  const packagesHtml = (listing.launchPackages ?? []).map((pack) => `<article class="card"><div class="tiny">${h(pack.startupPhase)}</div><h3>${h(pack.packageName)}</h3><p class="muted">${h(pack.fullDeliverables)}</p><span class="tag">${h(pack.launchPrice)}</span><span class="tag">${h(pack.eligibilityRequirement)}</span></article>`).join("");
  const proofHtml = (listing.integrationProof ?? []).map((proof) => `<li>${h(proof.type)} · ${h(proof.location)} · ${h(proof.status)} <span class="muted">${h(proof.experienceId)}</span></li>`).join("");
  return page(listing.name, `<section class="panel hero"><div><span class="pill">${h(listing.category)}</span><h1>${h(listing.name)}</h1><p class="muted">${h(listing.signal)}</p><div class="actions"><span class="tag">${h(listing.price)}</span><span class="tag">${h(listing.license)}</span><span class="tag">${h(listing.formats)}</span><span class="tag">Entrega: ${h(listing.delivery)}</span></div></div><div class="card"><div class="tiny">Enterprise fit</div><h2>${h(listing.buyer)}</h2><p class="muted">${h(listing.useCase)}</p></div></section><section class="panel section"><h2>AEC validation contract</h2><div class="grid c4"><article class="card"><div class="tiny">LOD</div><strong>${h(listing.lod)}</strong></article><article class="card"><div class="tiny">Units</div><strong>${h(listing.units)}</strong></article><article class="card"><div class="tiny">Tolerance</div><strong>${h(listing.tolerance)}</strong></article><article class="card"><div class="tiny">Coordinates</div><strong>${h(listing.coordinateSystem)}</strong></article></div></section><section class="panel section"><h2>Specialist QA matrix</h2><div class="grid c3"><article class="card"><div class="tiny">Source inputs</div><ul class="muted">${list(listing.sourceInputs)}</ul></article><article class="card"><div class="tiny">Validation checks</div><ul class="muted">${list(listing.validationChecks)}</ul></article><article class="card"><div class="tiny">Compliance signals</div><ul class="muted">${list(listing.complianceSignals)}</ul></article></div></section><section class="panel section"><h2>Delivery assets</h2><div class="grid c3">${listing.deliveryAssets.map((asset) => `<article class="card"><span class="status">Included</span><h3>${h(asset)}</h3></article>`).join("")}</div></section><section class="panel section"><h2>Launch packages</h2><div class="grid c3">${packagesHtml}</div></section><section class="panel section"><h2>Validation and integration</h2><ul class="muted" style="list-style:disc;padding-left:18px">${proofHtml}</ul></section>${nav}`);
}
